# Private planning doc — never committed (v402/verus-rpc convention, see RISKS.md)
PLAN.md

# Historical brainstorm scratch (superseded by DESIGN.md)
verus-client-brainstorm-prompt.md

node_modules/
dist/
coverage/
*.tsbuildinfo

# Real credentials never enter the repo — .env.example documents the shape
.env
.env.*
!.env.example

# Never commit key material or live wallet state (belongs in ~/.peculium)
*.keystore.json
keystore*.json
test-artifacts/

.DS_Store
